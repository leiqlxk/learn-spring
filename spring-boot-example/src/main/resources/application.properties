# 配置数据连接信息
spring.datasource.url=jdbc:mysql://119.23.249.145/sampledb
spring.datasource.username=root
spring.datasource.password=Zh@123456
spring.datasource.driver-class-name=com.mysql.jdbc.Driver

# 指定连接池，不指定则默认创建tomcat-jdbc连接池
# spring.datasource.type=org.apache.commons.dbcp2.BasicDataSource

# 连接池配置信息
spring.datasource.max-wait=10000
spring.datasource.max-active=50
spring.datasource.max-idle=10
spring.datasource.min-idle=8
spring.datasource.test-on-borrow=true
spring.datasource.validation-query=select 1

# 配置jndi数据源
# spring.datasource.jndi-name=java:comp/env/jdbc/sampleDs

# 初始化数据库脚本
#spring.datasource.initialize=true
#spring.datasource.platform=mysql
#spring.datasource.data=data
#spring.datasource.schema=schema

# 解析器配置
spring.mvc.view.prefix=/WEB-INF/jsp/
spring.mvc.view.suffix=.jsp

# 日志配置
logging.level.org.lql = DEBUG
logging.level.org.springframework.web = DEBUG
#logging.file = /var/netgloo_blog/logs/spring-boot-logging.log
